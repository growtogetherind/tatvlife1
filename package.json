{
  "name": "thewellmanco-monorepo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install-all": "npm install --prefix frontend --legacy-peer-deps",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
