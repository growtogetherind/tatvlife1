{
  "name": "tatvalife-monorepo",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install-all": "npm install --prefix frontend --legacy-peer-deps",
    "dev-frontend": "npm run dev --prefix frontend",
    "dev": "npm run dev-frontend",
    "build": "npm install --prefix frontend --legacy-peer-deps && npm run build --prefix frontend && rm -rf dist && cp -r frontend/dist dist"
  }
}
